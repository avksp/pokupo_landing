var WParameters = {
    core: {
        theme: '{{ theme }}',
        shopId: {{ shopId }},
        host: '{{host}}',
        pathToData : '{{pathToData}}',
        pathToPostData : '{{pathToPostData}}',
        hostApi : '{{hostApi}}'
    },
    modalMessage: {
        tmpl: {path: '{{protocol ~ '://' ~ url_tmpl_widgets}}/modalMessageTmpl.html'}
    }
}