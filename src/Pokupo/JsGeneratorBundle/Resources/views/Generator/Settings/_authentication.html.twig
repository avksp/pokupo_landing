{% if data|length > 0 %}
    WParameters.authentication = {
        {% if data.containerId is defined %}
            container: { widget: '{{ data.containerId }}' },
        {% endif %}
        {% if data.https is defined %}
            https: '{{ data.https }}',
        {% endif %}
        {% if data.useCustomTmpl is defined %}
            tmpl: { path: '{{ data.pathTmpl }}', id: '{{ data.idTmpl }}'},
        {% else %}
            tmpl: {path: '{{protocol ~ '://' ~ url_tmpl_widgets}}/authenticationTmpl.html'},
        {% endif %}
        animate: function (){}
    }
{% endif %}
