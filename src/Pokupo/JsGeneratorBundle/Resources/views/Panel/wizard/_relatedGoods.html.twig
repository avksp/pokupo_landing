{% trans_default_domain 'PokupoJsGeneratorBundle' %}
<li ng-class="{true: 'disabled'}[steps[{{ step }}].disabled]" ng-show="form[steps[{{ step }}].widget].related.active && steps[{{ step }}].show" class="ng-hide">
    <a href="#step{{ step }}" ng-click="Click.Tab({{ step }})">
        {{ 'relatedGoods.tab'|trans }}
    </a>
</li>