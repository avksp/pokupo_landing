{% trans_default_domain 'PokupoJsGeneratorBundle' %}
<li ng-class="{'disabled':steps[{{ step }}].disabled}" ng-show="steps[{{ step }}].show" class="ng-hide">
    <a href="#step{{ step }}" ng-click="Click.Tab({{ step }})">
        {{ 'choice.tab'|trans }}
    </a>
</li>